# Environment variables for LilSwap frontend
#
# Vite automatically loads:
# - .env.development (when running 'pnpm run dev')
# - .env.production (when running 'pnpm run build')
# - .env.local (for local overrides, never commit this file)
#
# Copy this to .env.local if you need to override any values locally

# Backend API URL
VITE_API_URL=http://localhost:3001/api/v1

# Environment mode
VITE_ENV=development