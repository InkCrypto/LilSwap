# Production Environment - LilSwap Frontend
# This file is loaded automatically by Vite in production builds

# Production backend API
VITE_API_URL=https://engine.TEMP_DOMAIN/api/v1

# Production mode flag
VITE_ENV=production

# Logging (only errors in production)
VITE_LOG_LEVEL=error
